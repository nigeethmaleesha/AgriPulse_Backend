server:
  port: 8084

spring:
  application:
    name: agripulse-backend
  datasource:
<<<<<<< HEAD:module-3-network/src/main/resources/application.yml
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:agripulse_module3_network}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:1234}
=======
    url: ${DB_URL:jdbc:postgresql://localhost:5432/agripulse_5}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:}
    driver-class-name: ${DB_DRIVER_CLASS_NAME:org.postgresql.Driver}
>>>>>>> 1cc0a7826f8ba4b36d29406eaa231bd3e5d7673d:src/main/resources/application.yml
  jpa:
    database-platform: ${DB_DIALECT:org.hibernate.dialect.PostgreSQLDialect}
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  jackson:
    serialization:
      write-dates-as-timestamps: false

agripulse:
  seed-demo: ${SEED_DEMO:true}

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}
